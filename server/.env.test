# Test Environment Variables
# This ensures tests use safe, isolated settings

NODE_ENV=test

# This will be overridden by MongoMemoryServer in tests
# But having it here ensures no accidental production connections
MONGO_URI=mongodb://localhost:27017/test-isolated-db

# Test JWT secret (different from production)
SECRET=test-jwt-secret-for-testing-only

# Test port (different from production)
PORT=3001
