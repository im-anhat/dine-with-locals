[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmd = 'cd server && npm install && npm install typescript'

[phases.build]
cmd = 'cd server && ./node_modules/.bin/tsc'

[start]
cmd = 'cd server && node dist/app.js'