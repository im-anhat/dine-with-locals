[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmd = 'cd server && npm ci'

[phases.build]
cmd = 'cd server && npm install typescript && ./node_modules/.bin/tsc'

[start]
cmd = 'cd server && node dist/app.js'